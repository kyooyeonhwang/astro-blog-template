{
  "name": "@example/starter",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build:sass": "sass src/scss:css",
    "watch:sass": "sass --watch src/scss:css",
    "start": "npm-run-all --parallel dev watch:sass",
    "build": "astro build",
    "preview": "astro preview"
  },
  "devDependencies": {
    "astro": "^1.0.0-beta.26",
    "npm-run-all": "^4.1.5",
    "sass": "^1.49.9"
  }
}
